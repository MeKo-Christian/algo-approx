{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "labels": ["dependencies"],
  "postUpdateOptions": ["gomodTidy"],
  "packageRules": [
    {
      "description": "Bump the algo-* family together. They are co-developed and an incompatible algo-fft can reach a consumer through two different paths at once, so splitting them across PRs produces combinations that never build.",
      "matchDatasources": ["go"],
      "matchPackageNames": ["/^github\\.com/cwbudde//"],
      "groupName": "cwbudde sibling modules",
      "schedule": ["at any time"],
      "prPriority": 10,
      "minimumReleaseAge": null
    },
    {
      "description": "Everything else can batch weekly; these are not the source of the drift problem.",
      "matchDatasources": ["go"],
      "excludePackageNames": ["/^github\\.com/cwbudde//"],
      "schedule": ["before 6am on monday"]
    }
  ],
  "vulnerabilityAlerts": {
    "labels": ["security"],
    "schedule": ["at any time"]
  }
}
